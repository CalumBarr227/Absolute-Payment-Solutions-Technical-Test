@page
@model TaskManager.Pages.EditModel

@{
    ViewData["Title"] = "Edit";
    Layout = "_Layout";
}

<h1>Edit</h1>

<h4>UserTask</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="UserTask.Id" />
            <div class="form-group">
                <label asp-for="UserTask.Title" class="control-label"></label>
                <input asp-for="UserTask.Title" class="form-control" />
                <span asp-validation-for="UserTask.Title" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="UserTask.Description" class="control-label"></label>
                <input asp-for="UserTask.Description" class="form-control" />
                <span asp-validation-for="UserTask.Description" class="text-danger"></span>
            </div>
            <div class="form-group form-check">
                <label class="form-check-label">
                    <input class="form-check-input" asp-for="UserTask.IsCompleted" /> @Html.DisplayNameFor(model => model.UserTask.IsCompleted)
                </label>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
